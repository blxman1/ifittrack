<resources>

    <!-- Base application theme - Modern Material Design -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Navigation bar style -->
        <item name="android:navigationBarColor">@color/SystemNavigationBarColor</item>

        <!-- Status bar style -->
        <item name="android:statusBarColor">@color/StatusBarColor</item>

        <!-- Window background -->
        <item name="android:windowBackground">@color/BackgroundSecondaryColor</item>

        <item name="android:windowDisablePreview">true</item>

        <!-- Default font family -->
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- Text input error color -->
    <style name="error_appearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/ErrorTextColor</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!-- Modern Card Style -->
    <style name="ModernCard">
        <item name="android:background">@drawable/card_style_01</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <!-- Primary Button Style -->
    <style name="PrimaryButton">
        <item name="android:background">@drawable/button_style_01</item>
        <item name="android:textColor">@color/ButtonTextColor</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">24dp</item>
        <item name="android:paddingRight">24dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Secondary Button Style -->
    <style name="SecondaryButton">
        <item name="android:background">@drawable/button_style_02</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">20dp</item>
        <item name="android:paddingRight">20dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Title Text Style -->
    <style name="TitleText">
        <item name="android:textColor">@color/TitleTextColor</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Section Header Style -->
    <style name="SectionHeader">
        <item name="android:textColor">@color/PrimaryTextColor</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Body Text Style -->
    <style name="BodyText">
        <item name="android:textColor">@color/PrimaryTextColor</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Caption Text Style -->
    <style name="CaptionText">
        <item name="android:textColor">@color/SecondaryTextColor</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!-- Large Number Style -->
    <style name="LargeNumber">
        <item name="android:textColor">@color/PrimaryTextColor</item>
        <item name="android:textSize">48sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Input Field Style -->
    <style name="InputField" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/input_style_01</item>
        <item name="android:textColor">@color/PrimaryTextColor</item>
        <item name="android:textColorHint">@color/HintTextColor</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">16dp</item>
    </style>

    <!-- Toolbar Title Style -->
    <style name="ToolbarTitle">
        <item name="android:textColor">@color/LightTextColor</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
